# CLAUDE.md

Legacy compatibility note for Claude Code contributors.

The maintained contributor instructions now live in `AGENTS.md`.

If you are working on this repository in Claude Code:
- Read `AGENTS.md` first.
- Treat `_CLAUDE.md` inside user vaults as a legacy compatibility file.
- Prefer `AGENTS.md` at the vault root for new Codex-first vault installs.

Claude-specific facts that still matter:
- Claude can use the optional PostCompact background hook in `hooks/obsidian-bg-agent.sh`.
- The repo still keeps `_CLAUDE.md` compatibility for users who remain on Claude surfaces.
